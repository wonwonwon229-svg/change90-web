export interface Review {
  name: string;
  role: string;
  content: string;
  rating: number;
}

export interface Feature {
  title: string;
  desc: string;
}

export interface FAQItem {
  question: string;
  answer: string;
}
